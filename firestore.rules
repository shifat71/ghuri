rules_version='2'

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Closed by default for security. 
      // Update these rules to match your application's data access patterns.
      allow read, write: if false;
    }
  }
}
